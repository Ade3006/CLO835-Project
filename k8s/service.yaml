apiVersion: v1
kind: Service
metadata:
  name: clo835-service
  namespace: final
spec:
  selector:
    app: clo835-app
  ports:
    - protocol: TCP
      port: 81
      targetPort: 81
  type: LoadBalancer
